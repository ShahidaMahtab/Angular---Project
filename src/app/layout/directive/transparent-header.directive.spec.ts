import { TransparentHeaderDirective } from './transparent-header.directive';

describe('TransparentHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new TransparentHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
